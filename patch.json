{
  "rationale": "Replace the raw SQL string with SQLAlchemy ORM usage so user-provided titles are safely parameterized while preserving functionality.",
  "patch": "*** Begin Patch\n*** Update File: /Users/justinexue/Downloads/flask-todo-app/app.py\n@@\n-    if title:\n-        sql = f\"INSERT INTO todo (title, complete) VALUES ('{title}', 0)\"\n-        db.session.execute(sql)\n-        db.session.commit()\n+    if title:\n+        todo = Todo(title=title, complete=False)\n+        db.session.add(todo)\n+        db.session.commit()\n*** End Patch\n"
}